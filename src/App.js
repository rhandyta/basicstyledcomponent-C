import Button from "./Components/Button";
import Wrapper from "./Components/Wrapper";

function App() {
    return (
        <Wrapper>
            <Button primary>Button Props</Button>
            <Button>Default</Button>
            <Button rounded>Default</Button>
        </Wrapper>
    );
}

export default App;
